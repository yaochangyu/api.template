# Skill: project-init (專案初始化)

## 📋 職責
檢測專案狀態、執行初始化對話、產生專案配置檔案

## 🎯 使用時機
- 首次接觸此專案
- 專案目錄為空或缺少關鍵檔案
- 需要重新初始化專案配置

## 🔍 檢測流程

### 1. 檢測專案狀態
檢查以下檔案/目錄是否存在：
- [ ] `env/.template-config.json` (配置檔)
- [ ] `*.sln` (解決方案檔案)
- [ ] `src/` 目錄且非空
- [ ] `appsettings.json` 或 `docker-compose.yml`

**判定條件**：滿足以上**任一**條件不存在 → 視為空白專案，進入初始化流程

### 2. GitHub 範本詢問（必須優先）
```
📌 是否使用 GitHub 範本？
- ✅ 是，使用 https://github.com/yaochangyu/api.template
- ❌ 否，從空白專案開始

選擇「是」時：
1. 檢查工作目錄是否為空
2. 如非空，詢問：改用子資料夾 / 取消
3. 執行: git clone https://github.com/yaochangyu/api.template .
4. 刪除: Remove-Item -Recurse -Force .git
```

### 3. 專案配置詢問（分階段進行）

#### 第一階段：資料庫配置
```
1️⃣ 資料庫類型選擇：
   - SQL Server (預設)
   - PostgreSQL
   - MySQL
   - SQLite (僅開發環境)

2️⃣ 資料庫版本：
   - SQL Server: 2022 (預設) / 2019 / 2017
   - PostgreSQL: 15 (預設) / 14 / 13
   - MySQL: 8.0 (預設) / 5.7

3️⃣ 是否使用 Entity Framework Core？
   - ✅ 是 (推薦)
   - ❌ 否 (使用 Dapper 或其他 ORM)
```

#### 第二階段：快取與基礎設施
```
1️⃣ 是否使用 Redis 快取？
   - ✅ 是 (推薦用於生產環境)
   - ❌ 否 (僅使用記憶體快取)

2️⃣ Redis 版本 (如選擇使用)：
   - 7-alpine (預設)
   - 7
   - 6-alpine
```

#### 第三階段：專案結構
```
專案組織方式：
1️⃣ 單一專案結構 (推薦小型團隊)
   - 所有層在 JobBank1111.Job.WebAPI 專案內
   - 優點：編譯快速、部署簡單
   - 適合：3 人以下團隊

2️⃣ 多專案結構 (推薦大型團隊)
   - Controller、Handler、Repository 各自獨立專案
   - 優點：職責清晰、便於協作
   - 適合：大型團隊、長期維護
```

### 4. 產生配置檔案
```json
// env/.template-config.json
{
  "database": {
    "type": "SQL Server",
    "version": "2022",
    "useEfCore": true
  },
  "cache": {
    "useRedis": true,
    "version": "7-alpine"
  },
  "projectOrganization": "single-project",
  "createdAt": "2025-01-03T05:33:24.583Z",
  "createdBy": "GitHub Copilot CLI"
}
```

## 🚫 禁止行為
- ❌ 不可擅自假設用戶選擇（即使有「預設」值）
- ❌ 不可跳過 GitHub 範本詢問步驟
- ❌ 不可一次詢問超過 4 個問題
- ❌ 不可在目錄非空時直接覆蓋

## ✅ 成功條件
- [x] 產生 `env/.template-config.json` 配置檔
- [x] 用戶明確選擇所有配置項目
- [x] 如使用範本，Git 歷史已清除
- [x] 專案結構符合用戶選擇

## 📚 參考文件
- 架構設計指南: [@references/architecture-guide.md](../references/architecture-guide.md)

## 💡 使用範例

```bash
# 使用此 skill
@project-init 請幫我初始化專案

# AI 回應範例
> 📌 偵測到這是空白專案，開始初始化流程...
> 
> 1️⃣ 是否使用 GitHub 範本？
>    - ✅ 是，使用 https://github.com/yaochangyu/api.template
>    - ❌ 否，從空白專案開始
```

## 🔗 相關 Skills
- `architecture` - 提供架構設計建議
- `database-ops` - 資料庫相關操作

## 🔗 相關 Agents
- `project-setup-agent` - 完整的專案設定流程（使用此 skill）
