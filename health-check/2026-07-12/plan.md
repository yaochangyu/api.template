# 修復執行計畫（2026-07-12 健檢輪）

**進度**: [計畫中]（本輪為診斷輪，以下步驟待使用者核准後於修復輪執行）
**前置**：全部工作在分支 `fix/health-check-20260712`（自 main 66e062c 起），不動 main。
**粒度**：每步可獨立派給 Opus/Sonnet 照做；「驗收」為機械可判定條件。步驟有依賴順序，S1→S2→S3 必須依序；S4–S7 可在 S3 後並行。

---

## S1. 升級 Mvc.Testing（解 P0-2）｜sonnet
1. `JobBank1111.Job.IntegrationTest.csproj:17` `Microsoft.AspNetCore.Mvc.Testing` 8.0.10 → 10.0.x 最新穩定；同檔 `Microsoft.Extensions.TimeProvider.Testing` 8.10.0 → 10.x（若無 10.x 用最新）。
2. `dotnet build` ＋ `dotnet test JobBank1111.Job.IntegrationTest`。

**驗收**：
- [ ] logs/aspnet-*.txt 不再出現 `PipeWriter.UnflushedBytes` 例外
- [ ] 「測試 API Header-1/-2」兩情境轉綠（GET /api/v1/tests 回 200）
- [ ] member 三情境的 server log 原始例外顯示為裸 `Unable to resolve service ... IMemberController`（不再被 PipeWriter 遮蔽）——預期仍紅，S2 才修

## S2. v2 收斂 Code First（解 P0-1＋P1，依 decisions.md D1）｜opus
1. `doc/openapi.yml`：刪除 `/api/v2/members`、`/api/v2/members:cursor`、`/api/v2/members:offset` 三個 path 定義（v1 paths 不動）。
2. 重跑 NSwag 生成管線（產生指令見 Taskfile / .claude 內生成說明；若無現成 task，找 `Contract/AutoGenerated` 檔頭註解的產生器版本與設定檔）。確認 `ContractControllers.cs` 不再含 `Contract.MemberController`（v2），`IMemberController` 介面同步消失或閒置。
3. 手寫 `Member/MemberController.cs` 路由對齊契約：`[Route("api/v2/members")]`；`:cursor`/`:offset` 用可產生 `api/v2/members:cursor`（無斜線）的 action 模板（如 `[HttpGet("~/api/v2/members:cursor")]`），與 BDD feature 的實際請求路徑一致。**測試檔不改**。
4. 若步驟 2 生成物中 `IMemberController` 仍存在且無人實作，確認 Program.cs 無殘留註冊需求；`Program.cs:64-66` 註解更新為實況。

**驗收**：
- [ ] `grep -rn "class MemberController" Contract/AutoGenerated/` 無 v2 生成 Controller
- [ ] `dotnet test JobBank1111.Job.IntegrationTest` **8/8 全綠**（本計畫最硬驗收）
- [ ] `git diff` 不含測試檔（飯粒.feature 及 steps）

## S3. 啟動期 Controller DI 契約測試（D8）｜sonnet
在 IntegrationTest 加一測試：反射列舉 WebAPI 組件所有非抽象 `ControllerBase` 衍生類，用 `ActivatorUtilities`/`IServiceProvider` 驗證建構子參數皆可解析。

**驗收**：
- [ ] 測試存在且在 S2 完成的分支上綠
- [ ] 反證測試：暫時註解掉 Program.cs:65 的 v1 註冊 → 該測試轉紅（貼輸出）→ 還原

## S4. 套件升級批 A（D3；解 Refit 以外全部漏洞）｜sonnet
EFCore 系、Caching.StackExchangeRedis、Caching.Memory、Data.SqlClient、System.Security.Cryptography.Xml、Microsoft.OpenApi、Azure.Identity、Microsoft.Identity.Client → 10.x／最新穩定（逐一以 nuget 確認相容 net10.0）。

**驗收**：
- [ ] `dotnet list package --vulnerable --include-transitive` 僅剩 Refit 相關
- [ ] build 0 error；單元＋整合測試不低於 S3 基準

## S5. Refit 升級 spike（D3 批 B；解 Critical）｜opus（spike）→ sonnet（套用）
1. spike：Refit(.HttpClientFactory) 7.2.1 → 13.1.0，重跑 Refitter 生成 `Contract/AutoGenerated`，比對生成 diff 與編譯錯誤，把「需要的適配修改」寫成明確步驟。
2. 套用並全測。

**驗收**：
- [ ] `--vulnerable` 零 Critical/High
- [ ] 整合測試 8/8；`JobClient` 相關功能有實跑證據
- [ ] spike 筆記寫入 health-check/2026-07-12/notes.md

## S6. P2 程式碼清理｜sonnet
1. `ActionResult.cs:18-38`：移除 dcbfa0f 診斷 try/catch，恢復簡潔版。
2. 刪除 `Member/MemberChain.cs`（死碼含 bug；刪前 `grep -rn "ValidateEmail\|ValidateName" --include="*.cs"` 確認呼叫端僅 MemberHandler 私有版本）。
3. header 解析去重：抽單一 helper（建議 static class，v1/v2 共用），行為統一為 TryParse＋fallback（v2 語意）。
4. `ExceptionHandlingMiddleware.CreateFailure`：非 Development 環境 Message 改泛用訊息（D5），保留 TraceId。
5. 刪 `Job.Test/UnitTest1.cs` 空測試（無 Assert）。

**驗收**：
- [ ] 全部測試不低於 S5 基準；4 的行為有整合測試或最小實跑證據（非 Dev 環境 500 回應不含例外原文）

## S7. 文件批修（D6）｜haiku（模式固定）→ 驗收由另一 agent
1. 連結修正（清單見 findings-docs.md §1-2，共 19+ 處；佔位符 xxx.md 段落整段移除或補實）。
2. tree.md 重產（176 檔基準，改動後以實際 git ls-files 為準）。
3. .archive/ 10 檔補 `⏹️ Status: COMPLETED` 標記；`P2.1-bdd-testing-PLAN.completed.md` 內文狀態人工複核後對齊檔名。
4. health-check/ 舊 4 檔檔頂加歷史橫幅（D6，不改內文）。
5. CLAUDE.md＋development-rules.md 加「範本例外」註記（D2）；README×2 的 best-practices.md 壞連結修正（指向現存對應文件或移除該行）。

**驗收**：
- [ ] 所有 md 相對連結 `test -e` 全過（docs-scan 同法複掃 0 壞連結）
- [ ] tree.md 自報數＝實列數＝git ls-files 數

## S8. 單元測試 Redis 隔離（D7）｜sonnet
CacheProviderFactoryTest 兩個 Redis 案例遷 IntegrationTest（Testcontainers Redis），或原地改 Testcontainers。

**驗收**：
- [ ] 本機**不開** Redis：`dotnet test JobBank1111.Job.Test` 全綠
- [ ] 兩案例在新位置有實跑通過證據

## S9. fresh-context 終驗（驗證不自驗）｜fresh opus
只給驗收條件清單＋分支名，逐條判定並引用證據行號；不透露執行過程。

**驗收**：
- [ ] S1–S8 逐條複驗全過；build/測試數字實跑；報告寫入 health-check/2026-07-12/final-verification.md

---

## 建議（不在本計畫內，待使用者決定）
- xunit 2.x → xunit.v3（legacy）；nullable warning ~108 筆分批清；tree.md 重產自動化成 task；CI 加 `--vulnerable` 門檻。
