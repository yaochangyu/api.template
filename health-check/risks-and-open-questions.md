# 風險與未解清單

日期：2026-07-04（健檢當下狀態）

## 需要使用者決策的未解問題
1. **v1 members 的定位**：v1 三端點已實作且有整合測試，但 openapi.yml 只有 v2。是「刻意不入規格的過渡版本」還是「漏補規格」？→ 決定 plan.md P7 做不做。
2. **是否繼續支援 GitHub Copilot**：repo 已把 agents/skills 遷到 .claude/，但 .github/copilot-instructions.md 還在。若放棄 Copilot，P5-3 從「收斂」改為「整組刪除」。
3. **EF 走向**：目前實務是 DB First（反向工程 scaffold），但 CLAUDE.md 承諾 migration 指令。P2 的 ef-migration-add/ef-database-update 是「補齊」還是「從文件移除」？兩者擇一。
4. **`.github/mcp-config.json`** 是否有自訂流程在讀？掃描判定無工具自動載入，但無法排除人工/CI 流程引用。
5. **staged 60 檔遷移何時 commit**（P0）：涉及 main，需使用者執行或授權。

## 已識別風險
1. **未 commit 的大遷移停在 staging**（高）：任何 `git reset --hard`/`checkout` 失誤會遺失整批搬移。這是目前 repo 最脆弱的一點。
2. **大小寫不敏感檔案系統遮蔽問題**（高，僅在散佈時爆發）：skill.md/SKILL.md 混用在本機看不出來，clone 到 Linux CI 才靜默失效；同理，未來任何純大小寫改名都必須 `git mv`。
3. **AutoGenerated 的修復路徑**（中）：P1 重產 ContractControllers.cs 依賴 refitter/nswag CLI 版本；版本不同可能產生大量無關 diff。建議先記錄工具版本再 codegen。
4. **文件合併的內容遺失風險**（中）：P5 合併四份規範文件時可能丟掉只存在某一份的細節；plan 已要求 diff 補差異，但仍屬人工判斷。
5. **129 個 build warning**（低）：多為 AutoGenerated 的 CS8618，不影響行為，但違反 CLAUDE.md「部署前無編譯警告」清單；手改 AutoGenerated 是禁止的，只能從 codegen 參數下手，效果未驗證。

## 我不確定 / 未驗證的事（誠實列）
- **未驗證** Claude Code 對小寫 skill.md 是否有 fallback（本 session 能看到 17 個 skill，但本機檔案系統大小寫不敏感，無法區分是 fallback 還是遮蔽）。
- **未驗證** B-1 的 500 行為未實際啟動 API 重現（僅靜態驗證 DI 註冊缺失：ContractControllers.cs:67 需要 ITagController、Program.cs 全檔無註冊；ASP.NET Core 對無法解析的 controller 依賴回 500 屬框架既定行為）。
- **未驗證** M-5（best-practices.md vs 最佳實踐.md）的實際重疊比例，掃描僅比對標題結構。
- **未驗證** integration test 8/8 是否涵蓋 v1 與 v2 全部端點（只確認測試全綠，未核對覆蓋範圍）。
- **未逐段比對** CLAUDE_OLD.md 與 CLAUDE.md 的全部差異（3702 行 vs 1092 行，抽查未見矛盾，獨有章節清單見 findings-docs.md F3）。
- build-test 子任務因 session 額度失敗，建置/測試由主對話實跑完成（結果有效，但「fresh-context 驗證」原則在本次由同一本體執行，P9 終驗時應補位）。
