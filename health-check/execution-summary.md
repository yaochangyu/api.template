# 修復執行紀錄（2026-07-05）

分支：`health-check-fixes`（自 main 的 b80f72b 起）。所有驗證均實跑。

## 各步驟狀態
| 步驟 | 狀態 | commit | 驗證證據 |
|---|---|---|---|
| P0 staged 遷移收斂 | ✅ | b80f72b（main） | 60 檔單獨 commit，unstaged 個人檔案未混入 |
| P1 移除 tags 殘留端點 | ✅ | abb5db9 | regen 後 grep ITagController=0；整合測試 8/8 |
| P2 Taskfile 補齊 | ✅ | e103ea2 | task build/test-unit 實跑過；缺 MIGRATION_NAME 明確報錯 |
| P3 CLAUDE.md 路徑修復 | ✅ | cbfa3b3 | 全檔相對連結 test -e 0 失效 |
| P4 SKILL.md 統一 | ✅ | 7c35c69 | git ls-files：小寫 0、大寫 17 |
| P5 文件收斂 | ✅（決策修訂） | 7c35c69 + 76cb819 | Copilot 整組刪除；CLAUDE_OLD 刪除不回填；best-practices 不合併改釐清定位 |
| P6 .claude/ 修正 | ✅ | 7c35c69 | 殘留引用 grep 0；範本 diff=0；命令範本路徑全存在 |
| P7 v1 入規格 | ✅ | abb5db9 | v1 改契約實作（MemberV1ControllerImpl）；測試全綠 |
| P8 收尾 | ✅ | e103ea2 + 76cb819 | Member1DbContext 刪除、.mcp.json 去硬編路徑、.template-config.json 建立、tree.md 重產（159 檔） |
| P9 fresh-context 終驗 | ✅（2026-07-07） | 本 commit | 新 session 逐條複驗 P0–P8 驗收條件全過；build 0 error/139 warning、單元 10/10、整合 8/8 均實跑；tree.md 過期（159→170）已重產；根層 .gitignore 已補（排除 .omc/） |

## 決策修訂（相對 decisions.md 初版）
1. **D1 修訂——CLAUDE_OLD 不回填直接刪**：抽查發現「獨有章節」判定有誤：Repository 章節其實已在 CLAUDE.md；EF 指令範例引用不存在的 task（回填會重演文件漂移）；日誌章節與現行內容重疊。git 歷史留底。
2. **D6 反轉——權威改為 .claude/templates/**：實際 diff 顯示 templates 版符合專案實況（{{ENTITY}}、實作產生介面），assets 版引用不存在的型別（JobBankDbContext、I{Feature}Api）。已把 assets 兩檔同步為 templates 內容（diff=0），維護規則寫入 .claude/README.md。repository-design 的 table/domain 兩版屬教學用途，保留不合併。
3. **M-5 修訂——兩份 best-practices 不合併**：章節比對顯示兩者定位不同（操作指南 vs 編碼原則），非重複；改為各加定位說明＋交叉連結，並把 best-practices.md 指令段對齊 Taskfile。
4. **新增——migration 參數改名 MIGRATION_NAME**：WSL 環境恆有 `NAME=<主機名>` 環境變數，會穿透 Taskfile 的 `requires` 檢查（實測誤觸發）。CLAUDE.md 與 best-practices.md 同步更新。

## 最終基準線（vs 健檢基準）
- build：0 error / 139 warning（基準 129；+10 淨值＝v1 codegen 新增的 AutoGenerated DTO 警告－Member1DbContext 移除；**手寫程式 0 warning**，已驗證）
- 單元測試：10/10（不變）
- 整合測試：8/8（不變）
- CLAUDE.md 連結：0 失效（基準 42 失效）

## 殘留與後續
- ~~P9 獨立終驗未執行~~ → 2026-07-07 完成（見上表）。終驗發現且已修的偏差：tree.md 未含 P8 之後新增的 11 檔（health-check/ 9 檔、.template-config.json、.refitter），已重產為 170 檔。
- `JobClient.cs` 的 regen diff 誤入 7c35c69 而非 abb5db9（內容正確、位置不影響結果）。
- 新檔 `Contract/AutoGenerated/.refitter`（refitter 2.0 產生的標記檔）已納管。
- ~~建議：根層加 `.gitignore` 排除 `.omc/`~~ → 2026-07-07 已加。
- push 與 MR 未執行（未經授權不推遠端）。
