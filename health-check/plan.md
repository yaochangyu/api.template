⚠️ **2026-07-12 註記**：本報告描述之分支 `health-check-fixes` 已於 4d5572a 併入 main 並刪除，內容為 2026-07-04 健檢輪的歷史紀錄。

---

# plan.md — 健檢修復執行計畫

粒度：每步可由 Opus/Sonnet 照做，附可機械判定的驗收。
前置共識：見 decisions.md（D1–D9）。除 P0 外，全部在分支 `health-check-fixes` 上進行，不直接動 main。
基準（修復前實測，2026-07-04）：`dotnet build` 0 error/129 warning；單元測試 10/10；整合測試 8/8。任何一步完成後這三個基準不得劣化。

---

## P0. 收斂目前的 staged 遷移（需使用者同意，因涉及 main）
- 動作：把目前 staged 的 60 檔 .github→.claude 遷移單獨 commit（訊息例：`[♻️] refactor(ai-config): 遷移 agents/skills 至 .claude/`）；unstaged 的 tree.md、settings.local.json 變更另行處理（tree.md 併入 P8 重產）。
- 驗收：`git status --short` 中 R/D 項全數消失；`git show --stat HEAD` 檔案數 ≈ 60。

## P1. 修復 blocker：移除 openapi 殘留 tags 端點（D5）
- 動作：
  1. 從 `dotnet-project-template/doc/openapi.yml:42-55` 移除 `/api/v2/tags:cursor`（GetTagsCursor，tag=Tag）。
  2. `cd dotnet-project-template && task codegen-api`（重產 client+server；需 refitter、nswag CLI）。
- 驗收：
  1. `grep -c "ITagController" src/be/JobBank1111.Job.WebAPI/Contract/AutoGenerated/ContractControllers.cs` 為 0。
  2. `dotnet build` 0 error。
  3. 啟動 API 後 `curl -s -o /dev/null -w "%{http_code}" "http://localhost:<port>/api/v2/tags:cursor?pageSize=1"` 回 404（原本會 500）。
  4. 整合測試 8/8 不變。

## P2. 補齊 Taskfile 五個缺失 task（D3）
- 動作：在 `dotnet-project-template/Taskfile.yml` 新增：
  - `build`: `dotnet build src/be/JobBank1111.Job.Management.sln`
  - `test-unit`: `dotnet test src/be/JobBank1111.Job.Test/JobBank1111.Job.Test.csproj`
  - `test-integration`: `dotnet test src/be/JobBank1111.Job.IntegrationTest/JobBank1111.Job.IntegrationTest.csproj`
  - `ef-migration-add`: `dir: src/be/JobBank1111.Job.DB`，`dotnet ef migrations add {{.NAME}}`（要求 NAME 參數）
  - `ef-database-update`: 同 dir，`dotnet ef database update`
- 驗收：`task build`、`task test-unit`、`task test-integration` 於 dotnet-project-template/ 下實跑通過；`task ef-migration-add` 缺 NAME 時給出明確錯誤而非靜默。
- 注意：ef 兩項需已安裝 dotnet-ef tool；若專案未用 migration（目前用反向工程），與使用者確認是否改為文件註明「本範本走 DB First，無 migration task」——擇一，不可兩者皆留。

## P3. CLAUDE.md 路徑與指令修正（D2）
- 動作：
  1. 全檔把 markdown 連結前綴 `project-template/` 換成 `dotnet-project-template/`（42 筆，行號清單見 findings-docs.md F1）。
  2. `doc/openapi.yml`、`docker-compose.yml`、`env/...` 的根層相對引用改為 `dotnet-project-template/` 前綴。
  3. 「常用指令」段與 P2 的 Taskfile 實況對齊，並註明執行目錄為 `dotnet-project-template/`。
- 驗收：以腳本抽出 CLAUDE.md 所有相對路徑連結逐一 `test -e`，失效數為 0。

## P4. skill 檔名統一（D4）
- 動作：9 個 `git mv skill.md SKILL.md`（api-development、bdd-testing、ef-core、error-handling、handler、middleware、project-init、repository-design、skill-agent-design）；`git rm .claude/skills/STRUCTURE-REPORT.md`。
- 驗收：`git ls-files '.claude/skills/*/skill.md'` 回 0 筆；`git ls-files '.claude/skills/*/SKILL.md'` 回 17 筆；新開 Claude Code session 確認 17 個 skill 全部出現在可用清單。

## P5. 文件收斂（D1、D7）
- 動作：
  1. 從 CLAUDE_OLD.md 回填至 CLAUDE.md（或其連結的文件）：EF migration 範例（:968-983，若 P2 採 migration 路線）、部署 YAML 精華（:3146-3517 摘要即可）、日誌與安全指引（:2534）；然後 `git rm CLAUDE_OLD.md`。
  2. 合併 `dotnet-project-template/best-practices.md` 與 `最佳實踐.md` 為一份（保留較完整者，diff 補差異），另一份刪除。
  3. Copilot：把根層 copilot-instructions.md 的「記錄 mcp/agent/skill」規則回填 `.github/copilot-instructions.md`（若使用者要保留該規則），刪根層版與 `.github/copilot-instructions0-old.md`。
- 驗收：`ls CLAUDE_OLD.md copilot-instructions.md .github/copilot-instructions0-old.md` 全部 not found；`grep -rl "project-template/" CLAUDE.md` 只剩 dotnet- 前綴；repo 內同主題規範檔 ≤ 1 份。

## P6. .claude/ 內容修正（D6）
- 動作：
  1. diff `.claude/templates/*.cs` 與 `.claude/skills/*/assets/*.cs`，擇優合併至 assets，刪 `.claude/templates/`，更新 `command-processor.md`、`commands/*.md` 中的範本路徑。
  2. skill-agent-design 與 skill-creator：改寫 `.github/skills|agents` 路徑為 `.claude/skills/<name>/SKILL.md`、`.claude/agents/<name>.md`；兩者擇一保留或合併（重疊度高，建議合併入 skill-creator）。
  3. `.claude/skills/templates/` 移至 `.claude/skills/security-fast-scan/assets/`（僅安全系列使用），更新五個 security skill 的引用。
  4. 更新 `.claude/README.md` 目錄樹（補 agents/、skills/、commands/jb.md）。
  5. `.claude/skills/*` 中殘留 `.copilot/mcp-config.json`、`doc/openapi.yml` 等失效引用逐一修正（清單見 findings-claude-dir.md 發現 3、5、6）。
- 驗收：`grep -rn "\.github/skills\|\.github/agents\|\.copilot/" .claude/` 為 0 筆；`.claude/templates/` 不存在；命令與 skill 引用的每個範本路徑 `test -e` 通過。

## P7. openapi 補 v1 members 規格（D5-2，待使用者確認 v1 定位後執行）
- 動作：在 openapi.yml 加入 `/api/v1/members:cursor`、`/api/v1/members:offset`、`POST /api/v1/members`（比照 MemberV1Controller 實際行為），重跑 `task codegen-api-client`。
- 驗收：Refitter 產出的 JobClient.cs 含 v1 members 方法；`dotnet build` 0 error；整合測試不紅。

## P8. 收尾雜項
- 動作：
  1. 建立 `dotnet-project-template/env/.template-config.json`（格式見 CLAUDE.md，database=SQL Server/useEfCore=true/cache.useRedis=true/projectOrganization=single-project），並把 CLAUDE.md 檢測章節的路徑敘述改為 `dotnet-project-template/env/`。
  2. Taskfile `ef-codegen-member` 移除第二條產生 `Member1DbContext` 的 scaffold，`git rm src/be/JobBank1111.Job.DB/AutoGenerated/Member1DbContext.cs`。
  3. `.mcp.json:13` 硬編路徑 `D:\lab\api.template` 改為相對路徑或環境變數。
  4. 處置 `.github/mcp-config.json`：無工具讀取，建議刪除（或搬為根層 `.mcp.json` 的一個 server 條目）。
  5. 重產 tree.md（`git ls-files` 為準），確認檔案總數與敘述一致。
  6. （可選）清 129 個 build warning：主要是 Contract/AutoGenerated 的 CS8618——由 NSwag 產生，優先調整 codegen 參數（如 nullable annotations）而非手改 AutoGenerated。
- 驗收：逐項 `test -e`/`grep`/`task` 驗證；最終 `dotnet build` warning 數 ≤ 基準 129（若做第 6 項則顯著下降）；單元 10/10、整合 8/8 不變。

## P9. 終驗（fresh-context 驗收，不由執行者自驗）
- 動作：派一個未參與修復的 agent，只給本檔各步驗收條件清單，逐條判定通過/不通過並引用證據行號。
- 驗收：全部通過；任何不通過項帶證據退回該步驟重做（計入重試）。
