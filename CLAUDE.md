# CLAUDE.md

此檔案為 Claude Code (claude.ai/code) 在此專案中工作時的指導文件。  
使用台灣用語的繁體中文回覆。

**📝 核心原則**: 詳細實作指南已移到 SKILL 系統（見末尾索引）。本檔案著重於決策邏輯與強制互動規則。

## 快速導航

- [AI 助理使用規則](#ai-助理使用規則)
- [開發指令](#開發指令)
- [架構概述](#架構概述)
- [強制詢問情境](#強制詢問情境)（1-5）
- [禁止的行為](#禁止的行為)（6 項）
- [進階主題索引](#進階主題索引)

---

## AI 助理使用規則

### 核心互動原則（強制遵守）

1. **強制互動確認**
   - **必須** 使用 `AskUserQuestion` 工具進行結構化詢問（Claude CLI）
   - 在所有需要使用者決策的情境下，都必須明確詢問，不得擅自執行
   - 提供清晰的選項說明，幫助使用者做出明智選擇

2. **不得擅自假設**
   - 即使文件標註「預設」值，仍須詢問使用者確認
   - 例外：使用者已在對話中明確指定（如「使用 SQL Server」）

3. **分階段互動**
   - 單次詢問最多 3-4 個問題，避免資訊過載
   - 複雜流程應分階段進行

4. **完整性優先**
   - 必須收集所有必要資訊後才開始執行

### 專案狀態檢測機制

首次接觸此專案時，檢測以下條件：
- ✅ 存在 `dotnet-project-template/env/.template-config.json`
- ✅ 存在 `.sln` 解決方案檔案與 `src/` 目錄
- ✅ 存在 `appsettings.json` 與 `docker-compose.yml`

若檢測失敗（空白專案），詢問是否使用 GitHub 範本 `https://github.com/yaochangyu/api.template`。

---

## 開發指令

### Taskfile 使用原則
- **優先使用 Taskfile**: 所有重複執行的開發指令應盡可能透過 `task` 命令執行
- **命令集中管理**: 複雜的多步驟指令應寫入 `Taskfile.yml`

### 常用指令
- **開發模式執行 API**: `task api-dev`
- **建置解決方案**: `task build`
- **執行單元測試**: `task test-unit`
- **執行整合測試**: `task test-integration`
- **產生 API 程式碼**: `task codegen-api`
- **從資料庫反向工程產生實體**: `task ef-codegen`
- **建立新的 Migration**: `task ef-migration-add MIGRATION_NAME=<MigrationName>`
- **更新資料庫**: `task ef-database-update`

**重要**: EF Core 相關指令必須透過 Taskfile 執行。

---

## 架構概述

### 核心專案
- **JobBank1111.Job.WebAPI**: 主要的 Web API 應用程式
- **JobBank1111.Infrastructure**: 跨領域基礎設施服務
- **JobBank1111.Job.DB**: Entity Framework Core 資料存取層
- **JobBank1111.Job.Contract**: 從 OpenAPI 規格自動產生的 API 客戶端合約

### 分層架構

**Controller → Handler → Repository** 模式：
- **Controller 層**: HTTP 請求/回應、路由、請求驗證、HTTP 狀態碼對應
- **Handler 層**: 核心業務邏輯、流程協調、錯誤處理與結果封裝
- **Repository 層**: 資料存取邏輯、EF Core 操作、資料庫查詢封裝

### 組織方式
- **方案 A：單一專案結構** — 所有功能層在 `JobBank1111.Job.WebAPI` 內（適合小型團隊）
- **方案 B：多專案結構** — Controller、Handler、Repository 各自獨立專案（適合大型團隊）

### 技術堆疊
- **框架**: ASP.NET Core 10
- **資料庫**: Entity Framework Core 與 SQL Server
- **快取**: Redis 搭配記憶體內快取備援
- **錯誤處理**: CSharpFunctionalExtensions (Result Pattern)
- **驗證**: FluentValidation
- **日誌記錄**: Serilog 結構化日誌
- **測試**: xUnit、Testcontainers、Reqnroll (BDD)
- **API 文件**: Swagger/OpenAPI 搭配 ReDoc、Scalar

### 程式碼產生工作流程
1. API 規格維護在 `doc/openapi.yml`
2. 使用 Refitter 產生客戶端程式碼至 `JobBank1111.Job.Contract`
3. 使用 NSwag 產生伺服器控制器至 `JobBank1111.Job.WebAPI/Contract`
4. 使用 EF Core 反向工程產生資料庫實體至 `JobBank1111.Job.DB`

**重要規範**: 
- 所有自動產生的程式碼都放在 `AutoGenerated` 資料夾中，不可手動編輯
- EF Core 反向工程與 Migration 必須透過 Taskfile 執行

---

## 強制詢問情境（必須詢問，不能跳過）

### 1️⃣ 專案初始化與配置
- 資料庫類型選擇（SQL Server / PostgreSQL / MySQL）
- Redis 快取需求（yes / no）
- 專案結構組織（single-project / multi-project）
- 是否使用 GitHub 範本

### 2️⃣ 資料庫相關操作
- Code First vs Database First 模式
- Migration 名稱與套用策略
- 資料表範圍選擇

### 3️⃣ 功能實作時必須詢問
**a) API 開發流程**
- ✅ API First（推薦）：定義 OpenAPI → 產生骨架 → 實作 → 文件同步
- ✅ Code First：直接實作 → 手動維護 OpenAPI

**b) OpenAPI 規格狀態**（僅 API First 時）
- 已定義 | 需要更新 | 尚未定義

**c) 需要實作的分層**
- Controller | Handler | Repository（可多選）

### 4️⃣ **測試策略詢問**（最重要，不能假設）
**問 a)：是否需要測試？**
- ✅ 完整測試（BDD + 單元測試）
- ✅ 僅 BDD 整合測試
- ✅ 僅單元測試
- ❌ 暫不實作

**問 b)：測試範圍**
- 完整功能 | 核心業務邏輯 | 關鍵路徑 | 異常情境

**問 c)：BDD 情境**（若選擇 BDD）
- 已有 .feature 檔案？
- 需要新增哪些情境？
- 需要 AI 協助 Gherkin 語法？

**問 d)：測試環境**
- Docker 容器（SQL Server / Redis）
- Seed Data | 動態產生 | 清理策略

**問 e)：測試方法**
- ✅ API 端點必須 BDD 測試（.feature + Reqnroll）
- ✅ 優先 Testcontainers（Docker）作為測試替身
- ✅ 禁止直接測試 Controller

### 5️⃣ 效能最佳化
- 優化面向選擇（資料庫查詢 / 快取 / 非同步 / 記憶體）

---

## 禁止的行為 ❌

1. ❌ 擅自使用預設值 — 必須明確詢問
2. ❌ 跳過詢問步驟 — 即使有推薦選項，仍須確認
3. ❌ 一次詢問過多問題 — 最多 3-4 個
4. ❌ 提供不明確的選項 — 必須加說明
5. ❌ 假設測試需求 — 不可假設，必須明確詢問
6. ❌ 跳過測試實作詢問 — 實作新功能時必須詢問測試策略

---

## 進階主題索引

當實作具體功能時，根據需求查閱相應 SKILL（詳細實作指南已從 CLAUDE.md 移出）：

| 工作項 | 相關 SKILL | 說明 |
|--------|-----------|------|
| 設計 API 端點 | `/api-development` | OpenAPI First vs Code First 流程、規格定義 |
| 實作 Controller | `/handler` | 主建構函式注入、依賴注入最佳實踐、Result Pattern |
| 設計資料存取 | `/repository-design` | 資料表導向 vs 需求導向 vs 混合策略、決策檢查清單 |
| 處理錯誤 | `/error-handling` | Result Pattern、Failure 物件、分層錯誤處理 |
| 實作中介軟體 | `/middleware` | TraceContext 管理、Exception Handling、Request Logging |
| EF Core 最佳化 | `/ef-core` | 查詢最佳化、Migration、DbContextFactory 模式 |
| 快取設計 | `/caching-strategy` | 多層快取（L1 Memory / L2 Redis）、失效策略 |
| 測試實作 | `/bdd-testing` | Gherkin、Reqnroll、Testcontainers、Docker 測試環境 |
| 監控與可觀測性 | `/observability` (P1) | 健康檢查、Seq、OpenTelemetry 整合 |
| 安全性檢查 | `/security-check-config` | 設定安全、依賴檢查、機密掃描 |

---

## 計畫書生命週期管理

當建立新計畫書時，遵循以下流程：

### 建立階段
- 檔案名格式: `{project}-PLAN.md` 或 `{project}-plan.md`
- 位置: 專案根目錄（方便檢視）
- 開頭標記: `**計畫時間**: YYYY-MM-DD HH:mm GMT+8` 與 `**進度**: [計畫中]`

### 執行階段
- 即時更新進度追蹤表
- 每完成一個 Step，標記狀態為 ✅ 與進度百分比

### 封存階段（計畫全部完成後）
1. **移到 archive 目錄**: `mv {project}-PLAN.md .archive/` 或 `mv {project}-plan.md .archive/`
2. **在檔案頂部加標記**:
   ```markdown
   ⏹️ **Status**: COMPLETED (YYYY-MM-DD HH:mm GMT+8)
   
   此計畫已全部執行完成，已封存至 .archive/
   詳見 Git commit: {commit-sha}
   ```
3. **Git commit** — commit message 中**不包含 Co-authored-by**（遵守全域規則）

---

## 最佳實踐檢查清單

### 開發前
- [ ] 檢查專案狀態（是否需要初始化）
- [ ] **【必須】詢問 API 開發流程選擇**（API First vs Code First）
- [ ] **【必須】詢問測試策略與範圍**（不可假設）
- [ ] 架構決策確認

### 開發中
- [ ] 所有 I/O 操作使用 async/await
- [ ] 傳遞 CancellationToken
- [ ] 使用 Result Pattern 處理錯誤
- [ ] 使用 DbContextFactory 而非直接注入 DbContext
- [ ] 主建構函式注入（Primary Constructor）
- [ ] 使用結構化日誌（Serilog）

### 測試（依使用者選擇執行）
- [ ] **確認已詢問使用者測試需求**
- [ ] 透過 BDD 情境測試 API（若需要 BDD）
- [ ] 實作單元測試（若需要單元測試）
- [ ] 使用 Docker 測試環境（Testcontainers）
- [ ] 避免直接測試 Controller
- [ ] 測試涵蓋核心業務邏輯與異常情境

### 部署前
- [ ] 檢查機密未存放在 appsettings.json
- [ ] 所有測試通過（若有實作測試）
- [ ] 無編譯警告
- [ ] 文件已更新
- [ ] Code Review 完成

---

**文件版本**: 3.0（精簡版，決策邏輯優先）  
**最後更新**: 2026-07-10 18:05 GMT+8
