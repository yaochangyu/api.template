# 簡單 .feature 範本
# 說明：複製此檔案並填入您的功能需求

Feature: [功能名稱]
  [功能的簡短描述]
  [為什麼這個功能重要（業務價值）]

  # 背景條件（所有情境共享的前置條件）
  Background:
    Given [共通的初始狀態1]
    And [共通的初始狀態2]

  # 情境 1：主要成功路徑
  Scenario: [情境標題 - 描述成功情況]
    # Given - 建立初始狀態
    Given [角色/使用者]具有/擁有[初始狀態]
    And [其他初始狀態]

    # When - 執行動作
    When [角色/使用者]執行[動作]
    And [其他相關動作]

    # Then - 驗證結果
    Then [系統]應該[預期結果]
    And [其他預期結果]

  # 情境 2：替代路徑或異常情況
  Scenario: [情境標題 - 描述異常情況]
    Given [初始狀態]
    When [執行動作]
    Then [驗證異常結果]

  # 情境 3：邊界條件
  Scenario: [情境標題 - 描述邊界情況]
    Given [初始狀態]
    When [邊界情況下的動作]
    Then [邊界情況下的結果]

  # 情境大綱（多組資料測試同一行為）
  Scenario Outline: [情境標題 - 使用參數變化]
    Given [使用 &lt;parameter1&gt;]
    When [執行涉及 &lt;parameter2&gt; 的動作]
    Then [結果為 &lt;expected&gt;]

    Examples: [場景名稱1]
      | parameter1 | parameter2 | expected  |
      | 值1        | 值2        | 預期結果1 |
      | 值3        | 值4        | 預期結果2 |

# 撰寫技巧
# 1. 情境標題應描述「做什麼」而非「怎麼做」
# 2. Given 應描述狀態而非動作
# 3. When 應包含一個主要動作
# 4. Then 應驗證可觀察的結果
# 5. 使用第三人稱現在式
# 6. 每個情境 3-5 步為佳，最多 10 步
# 7. 優先宣告式（高層次）而非命令式（實作細節）
